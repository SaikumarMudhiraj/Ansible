- name: ping the server
  hosts: frontend
  tasks: # list of the tasks
  - name: ping the server
  ansible.builtin.ping:

  - name: ping the server
  hosts: frontend
  tasks: # list of the tasks
  - name: ping the server
  ansible.builtin.ping:
  

